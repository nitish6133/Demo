import UserMenu from "../components/UserMenu";

const Booking = () => {

    return (
        <div>
            <div className="flex justify-between items-center p-4 bg-gray-100">
                <h1>Booking / first booking</h1>
                <UserMenu />
            </div>
        </div>
    );
};

export default Booking;