export interface GeneratedItem {
  id: string;
  studentName: string;
  studentClass: string;
  profession: string;
  futureImageUrl: string;
  finalVideoUrl?: string;
  createdAt: Date;
  isPosted: boolean;
}